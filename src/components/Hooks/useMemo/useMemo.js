import styles from "../hooks.module.scss";
import { useMemo } from "react";

function ApplicationUseMemo() {
  return (
    <div className={styles.container}>
      <p>ApplicationUseMemo</p>
    </div>
  );
}

export default ApplicationUseMemo;
