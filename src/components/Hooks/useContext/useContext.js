import styles from "../hooks.module.scss";
import { useContext } from "react";

function ApplicationUseContext() {
  return (
    <div className={styles.container}>
      <p>ApplicationUseContext</p>
    </div>
  );
}

export default ApplicationUseContext;
