import styles from "../hooks.module.scss";
import { useReducer } from "react";

function ApplicationUseReducer() {
  return (
    <div className={styles.container}>
      <p>ApplicationUseReducer</p>
    </div>
  );
}

export default ApplicationUseReducer;
