import styles from "../hooks.module.scss";
import { useRef } from "react";

function ApplicationUseRef() {
  return (
    <div className={styles.container}>
      <p>ApplicationUseRef</p>
    </div>
  );
}

export default ApplicationUseRef;
