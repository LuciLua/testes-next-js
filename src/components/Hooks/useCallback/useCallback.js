import styles from "../hooks.module.scss";
import { useCallback } from "react";

function ApplicationUseCallback() {
  return (
    <div className={styles.container}>
      <p>ApplicationUseCallback</p>
    </div>
  );
}

export default ApplicationUseCallback;
